package com.ozantech.ozantechcaseapp.core.model.extension

object EntityExt {
    /*    fun PairDetailResponse.toEntity() = PairDetailEntity(
            pair = pair,
            pairNormalized = pairNormalized,
            high = high,
            last = last,
            low = low
        )

        fun PairResponse.toEntity() = PairEntity(
            pair = pair,
            pairNormalized = pairNormalized,
            volume = volume,
            dailyPercent = dailyPercent,
            last = last,
            numeratorSymbol = numeratorSymbol,
            isFavorite = isFavorite
        )

        fun List<PairResponse>.toEntityList(): List<PairEntity> = map {
            it.toEntity()
        }*/
}